<?php

namespace Application\ChiaBundle\Entity;

use Doctrine\ORM\EntityRepository;

/**
 * TaskCategoryRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class TaskCategoryRepository extends EntityRepository
{
    public function getAll()
    {
        return $this->_em->createQuery('SELECT c FROM ChiaBundle:TaskCategory c ORDER BY c.name')
            ->getArrayResult();
    }

    public function getCategoryOptions()
    {
        $categories = $this->_em->createQuery('SELECT c FROM ChiaBundle:TaskCategory c ORDER BY c.name')
            ->getArrayResult();
        $category_choices = array();
        foreach ($categories as $category) {
            $category_choices[$category['id']] = $category['name'];
        }
        return $category_choices;
    }
}
