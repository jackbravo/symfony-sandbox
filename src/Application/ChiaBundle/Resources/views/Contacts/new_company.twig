{% extends "ChiaBundle::layout.twig" %}

{% block content %}
    <h1>{% block title 'New company' %}</h1>
    <form action="{{ path('companies_new') }}" method="post" {{ form_enctype(form) }}>
        {{ form_field(form) }}
        <input type="submit" value="Send!" />
        <a href="{{ path('contacts') }}">Cancel</a>
    </form>
{% endblock %}
