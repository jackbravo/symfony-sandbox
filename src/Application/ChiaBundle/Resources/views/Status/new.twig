{% extends "ChiaBundle::layout.twig" %}

{% block content %}
    <form action="{{ path('status_new') }}" method="post" {{ form_enctype(form) }}>
        {{ form_field(form) }}
        <input type="submit" value="Send!" />
        <a href="{{ path('status') }}">Cancel</a>
    </form>
{% endblock %}
